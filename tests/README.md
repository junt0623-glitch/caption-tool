# テストスイートについて

「展覧会キャプション工房」（単一ファイル `index.html`）の回帰テスト一式です。
Playwright でブラウザを実際に起動し、`index.html` をローカルファイルとして開いて操作・検証します。サーバーは不要です。

## セットアップ（初回のみ）

```bash
npm install
npx playwright install chromium
```

## 全テストを一括実行

```bash
npm test
```

または直接:

```bash
node tests/run-all.js
```

`tests/` 配下の `bt*.js` を番号順にすべて実行し、最後に合計の合否件数を表示します。

## 1本だけ実行

```bash
node tests/bt05_layout_override_diff.js
```

失敗すると `✗ FAIL: ...` の行が出力され、exit code が 1 になります（CI 等での判定に利用可）。

## テストファイル一覧

| ファイル | 内容 |
|---|---|
| bt01_load_defaults.js | 起動直後の既定状態（タブ・モード・ズーム・《》既定値） |
| bt02_project_management.js | 展覧会の新規作成・名称変更・削除 |
| bt03_work_crud.js | 作品の追加・編集・削除・検索フィルタ |
| bt04_mode_switch_lock.js | マスター編集／個別編集の切替と体裁ロック |
| bt05_layout_override_diff.js | 作品ごとの配置上書き（差分方式：触れた項目だけ独自化） |
| bt06_drag_align.js | 項目のドラッグ移動・選択・整列 |
| bt07_image_import.js | 画像の取り込み（個別／マスター一括）・削除 |
| bt08_master_image_sample.js | マスター編集モードでの画像見本プレビュー |
| bt09_image_wheel_zoom.js | 画像上でのマウスホイールによる拡大縮小 |
| bt10_image_drag_handle.js | 画像のドラッグハンドルによる拡縮・位置移動 |
| bt11_preview_zoom.js | 編集プレビューのズーム（実寸100%既定・拡大縮小・画面に収める） |
| bt12_bracket_default.js | 作品名の《》既定値（新規はOFF・既存データは維持） |
| bt13_backup_badge.js | JSONバックアップ推奨バッジ（配置上書き多用時の注意喚起） |
| bt14_undo_redo.js | 元に戻す／やり直す |
| bt15_json_export_import.js | JSON書き出し・読み込み |
| bt16_bulk_operations.js | 台帳の一括選択・一括画像登録／削除 |
| bt17_print_and_inline_edit.js | 印刷タブの描画・キャンバス上でのインラインテキスト編集 |
| bt28_extra_images_online_fonts.js | オンラインフォント拡充・選択時の即時読み込み反映／追加画像の複数貼り付け（Ctrl+V・ファイル）・移動・拡縮・削除・他作品への同位置一括コピー |
| bt29_renumber_on_move.js | 台帳の▲▼並び替えで番号が行の位置に合わせて自動で振り直されること（空番号・Undo含む） |
| bt30_csv_export.js | 作品リストのCSV書き出し（BOM・エスケープ）・表コピー（TSV）・書き出し→再取り込みの往復 |
| bt31_dialog_draggable.js | 「作品の編集」等のダイアログをタイトルバーのドラッグで移動できること（閉じるボタンとの誤認防止・再オープン時に中央へ復帰・画面中央への初期表示・タッチ操作での最後までのドラッグ） |
| bt32_item_panel_draggable.js | 編集キャンバスの項目クリックで開く書式の小パネルをヘッダーのドラッグで移動できること（閉じる✕との誤認防止・再オープン時に右上へ復帰・書式変更で再描画されても位置維持・タッチ操作でのドラッグ） |
| bt33_added_system_fonts.js | 追加システムフォント（UDデジタル教科書体 標準／太字・MS UI Gothic）の登録・選択肢表示・Win11 24H2統合名(UD デジタル 教科書体 NP)を最優先＋font-weightで太さ指定・旧-R/-B名の後方互換・書体名の監査（実在フォント名） |
| bt34_transparent_png.js | 透過PNGを取り込んでも透明部分が黒くならない（アルファ有→PNG保存・透過保持／アルファ無→JPEG圧縮） |
| bt35_export_filename.js | 書き出しファイル名（JSON・CSV）に展覧会名＋日付が自動で入ること（名称変更の追随・禁止文字の置換・空名時の補完） |
| bt36_print_actual_size.js | 印刷ボタンで原寸印刷の案内→プリンター詳細設定（印刷ダイアログ）へ進む流れ・原寸@page設定・キャンセル・「次回から表示しない」の保存と直接印刷 |
| bt37_font_diagnosis.js | フォント診断（isFontAvailableの陽性/陰性判定・未インストール書体選択時の警告・候補フォント利用可否の一覧表示・オンライン書体は警告対象外） |
| bt38_windows_fonts.js | Windows標準搭載フォントの網羅追加（「欧文（Windows標準）」グループ・Arial/Calibri/Consolas等の登録と適用・游ゴシックUI/メイリオUI/BIZ UD等幅の追加・診断連携） |
| bt39_print_actual_size_fix.js | 原寸印刷の縮小対策（シートが用紙寸法を超えない安全マージン・A4等の用紙名@page指定・原寸確認用100mm目盛りの実寸/刻み/保存・印刷CSSのページ超過防止） |
| bt40_no_pageno.js | 印刷シート右下の見出し（展覧会名＋ページ番号）を廃止し、プレビュー・印刷DOM・複数枚のいずれにも含まれないこと |
| bt41_tight_safe_edge.js | 詰めて並べる（tight）モードで外周の枠線が印字不可領域に入らないこと（四辺とも安全余白5mm以上・用紙中央配置・取り数維持・小さい札でも成立） |
| bt42_architecture_backgrounds.js | 建築テーマの背景3案（斗栱帯・重層楼閣・連子窓）の登録・カテゴリ・灰陶/緑釉の地色・ピッカー表示・適用時の描画・柄の大きさと濃さへの追随・印刷反映 |
| bt43_architecture_backgrounds2.js | 建築テーマの背景4案の追加（瓦当文・幾何の組物の上部帯／五層望楼・仏塔線描の左端縦帯16mm）の登録・ID重複なし・スウォッチ・帯の向きと幅・SVGマスクでも柄の色/大きさ/濃さが効くこと・印刷反映／廃止した「釉だまり」の除去と旧IDの読み替え |
| bt44_ruby_all_jp_items.js | 日本語の項目すべて（番号・よみがな・作品名・産地（作者）・時代・所蔵・キャッチコピー・解説文）にルビを振れること・英訳の項目は対象外・ルビ欄からの適用/解除・スペース区切りのモノルビ・保存と再読込・印刷反映 |
| bt45_kiniro_font.js | GN-きんいろサンセリフの追加（ゴシック体グループへの登録・和文名/欧文登録名の両方をスタックに持つこと・適用と印刷反映・未インストール時の警告が代替書体で潰れないこと＝probeによる判定範囲の切り分け） |
| bt46_char_scale.js | 文字の縦横幅（長体・平体、1〜200%）の項目ごとの指定・既定100%と既存データの補完・字形だけ伸縮し枠の実寸は不変・はみ出し判定/選択枠が見た目の実寸で測られること・範囲の丸め・解説文の自動縮小との併用・作品ごとの上書き・保存と再読込・印刷反映 |
| bt47_number_inputs.js | 数値の調整を全項目で「直接入力＋上下ボタン」にそろえたこと（全スライダーに数値欄・全数値欄に▲▼が1組・直接入力と1目盛り増減の反映・小数刻みの桁・上下限での丸め・スライダーと数値欄の相互追随・ズームと選択文字の字間・入力中に打ちかけを消さないこと） |
| bt48_excel_import.js | 作品リストをExcelファイル（.xlsx）から取り込めること（共有文字列・インライン文字列・数値・日付書式・複数シートの切替・列の自動判定）／CSV・TSVファイルの読み込み（UTF-8 BOM・Shift_JIS・引用符付きセル）／貼り付けとの優先関係・壊れたファイルと旧形式(.xls)の案内・書き出し→読み戻しの往復 |
| bt49_item_lineheight_ruby_fit.js | 項目ごとの行間（キャッチコピー等・既定は「全体に従う」・入力と全体への復帰・高さへの反映・作品ごとの上書き・印刷反映）／単漢字ルビを親文字の幅に収める掛け方（通常との選択・送り幅が広がらないこと・長体の倍率と下限50%・縦書きは従来どおり・印刷反映） |
| bt50_drive_json.js | JSONをGoogleドライブから直接読み込めること（接続設定のダイアログ・drive.fileスコープ限定・未設定時は設定を先に開く・片方だけの入力は保存しない・図面アプリの設定の引き継ぎ・取り込み経路がファイル選択と共通・不正なJSONの拒否・接続失敗時に設定を開き直せること） |
| bt51_char_font_size.js | 編集中の文字サイズ調整（項目全体と選んだ文字だけの2通り・現在値の表示・編集を中断せず即時反映・確定後と印刷への反映・字間と大きさの併用で片方が消えないこと・空欄で全体に戻せること・4〜60ptの丸め・「選択範囲を既定に戻す」での解除） |
| bt52_per_work_size.js | 札の寸法を作品ごとに持てること（「この作品だけ」で寸法を変えても他作品とマスターに及ばない・マスター編集では従来どおり全体に効く・「マスターに戻す」での解除・台帳でチェックした作品だけへの一括設定と解除・寸法が混在した印刷で重ならずに並ぶこと・均一なら従来の格子のまま・保存と再読込） |
| bt53_one_mode_multiselect.js | 作品ごとの編集で上書きOFFのとき、小パネルと整列バーの扱いがそろっていること（どちらも触れない・理由の案内と解除ボタン・選択を保ったまま編集へ進める・解除後は複数選択に操作が効きマスターと他作品に及ばない・マスター編集は従来どおり） |
| bt54_ime_enter.js | 日本語入力の変換確定のEnterでルビ入力が終了しないこと（確定のkeydownに変換中の印が付く場合／compositionendが先に来る場合の両方・確定後のEnterでルビが振られて閉じること・変換を挟まないEnterは従来どおり・変換中のEscapeで編集や選択が解除されないこと） |
| bt55_per_work_text_style.js | 行間・書体・ルビの体裁・表示する項目を作品ごとに持てること（「この作品だけ」で行間を変えてもマスターと他作品に及ばない・触れた項目だけの差分でマスターの後からの変更に追随する・マスター編集では従来どおり全作品に効く・余白／縦書き／体裁の複製は作品ごとに持てないので個別編集中は閉じて理由を示すこと・印刷反映・「マスターに戻す」での解除・保存と再読込） |
| bt56_print_preview_parity.js | 作ったもの・書き出して読み直したもの・印刷の三者で、画像／テキスト／ルビの位置がmm単位で一致すること（キャプション面と解説面×縦横書きの全組み合わせ・印刷用CSSを当てても動かないこと・表示倍率を変えても組版が変わらないこと・印刷プレビューの縮小表示と実印刷の一致・「収める」ルビが親文字からはみ出さないこと・JSON往復で体裁データが1文字も変わらないこと・作品ごとの上書きを持つデータの往復・読み込み時の上書き層の正規化） |
| bt57_print_outline_text.js | 印刷時にテキストを画像に変換して出せること（アウトライン化の代わり・既定OFF・300/600/1200dpiの選択と画素数への反映・文字だけが画像になり背景デザインと写真は元のまま・縦書き／解説面でも成立・焼き付けた文字が元の文字と1画素（約0.26mm）も動かないこと＝実際に描かせた絵を行ごとに突き合わせて確認・オンラインフォント使用時の注意書き・設定の保存と再読込・想定外の解像度の丸め／印刷プレビューでも画像化した状態で確かめられること＝進み具合の案内・解像度を変えたときの焼き直し・拡大縮小しても保たれること・変換の途中で切り替えたときの打ち切り・プレビューで見た画像がそのまま印刷に使われること） |
| bt58_fit_desc_editing.js | 枠に収めた解説文が、編集を始めても組みが変わらないこと（収めるための字間・長体が編集中も外れない・行の折り返しも変わらない・打ち込んでいる間も確定後と同じ組みで収め続ける・編集を終えた瞬間に組みが変わらない・打った文字が重複も欠落もせず保存される・読み直しても収めた状態で再開する・変換中は組み直さない・キャプション面の編集は従来どおり） |
| bt59_per_work_isolation.js | 【総点検】作品ごとの編集がマスターにも他の作品にも干渉しないこと（配置・寸法・書体/行間・ルビの体裁・表示項目・背景/画像・白い敷き・文章・ルビ・文字単位の書式・追加画像・解説面の自動調整まで一通り操作し、編集した作品以外が1つも変わらないことを項目単位で突き合わせ／作品を切り替えても漏れない・マスター編集は全作品に効くが独自の作品は引きずられない・触れていない項目はマスターの変更に追随する・上書きOFFでは体裁に触れない・読み直しても分かれ方が変わらない・「マスターに戻す」で解除してもマスターと他作品は不変） |
| bt60_ruby_follows_text_edit.js | ルビを振ったあと本文を編集しても、ルビが同じ文字に付いたままであること（キャンバスでの打ち込み・削除／作品の編集ダイアログからの書き換え／先頭・中ほど・末尾への追加・削除・言い換え・ルビのすぐ前後に足してもルビが伸びないこと・消した文字のルビだけ外れること・本文をそっくり差し替えたら行き場の無いルビを残さないこと・文字ごとの書式と縦中横も同じ扱い・作品名など他の項目でも同じ・保存と再読込） |
| bt61_duplicate_works.js | 作品台帳で選んだ作品をコピーしてリストに加えられること（それぞれの1つ下に入る・コピー側の番号だけ空になる・文章／章／画像／追加画像／ルビ／文字ごとの書式／作品ごとの配置・寸法・書体・背景をすべて引き継ぐ・追加画像の目印は振り直す・コピーと元が別物として独立している・コピーした行が選ばれた状態になる・見ていた作品を見失わない・「元に戻す」で取り消せる・1件／続けて2回／全件・絞り込み中でも正しい位置に入る・保存と再読込） |

補助: `tests/xlsxfix.js` は、テスト用に実物と同じ構造の .xlsx（ZIP＋XML・sharedStrings・styles・複数シート）を組み立てるヘルパーです。

## 新機能を追加した時のテスト追加手順

1. `tests/helpers.js` の `openApp()` / `mkRunner()` を使い、`btNN_機能名.js` という名前で新規ファイルを作成する
2. 末尾に以下を必ず入れる（単体実行と run-all 両対応のため）:
   ```js
   module.exports = { run };
   if (require.main === module) { run().then(r => process.exit(r.fail ? 1 : 0)); }
   ```
3. `node tests/btNN_機能名.js` で単体実行して確認
4. `npm test` で全体に影響が無いか確認してからコミットする

## 注意点（ハマりどころ）

- `confirm()` や `alert()` のうち、**ブラウザ標準のダイアログ**は `page.once('dialog', d => d.accept())` 等で処理する
- 一方、本アプリの「削除」や「マスター一括登録」などの確認は **独自実装のダイアログ**（`#confirmDialog` / `#confirmOk`）なので、`page.click('#confirmOk')` で閉じる必要がある（ネイティブdialogイベントは発火しない）
- 各テストは `browser.newContext()` で毎回まっさらな `localStorage` から始まる（テスト間の状態汚染を防ぐため）
